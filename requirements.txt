# ComfyUI normally provides these already.
numpy
Pillow
# Used for video metadata and the CPU compatibility decoder.
opencv-python
# Used to locate ffmpeg when it is not already on PATH.
imageio-ffmpeg
# DirectML ONNX Runtime is bundled as official wheels for Python 3.10-3.13.
